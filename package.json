{
  "name": "pnwnelson-resume",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "boostrap": "^1.0.0",
    "react": "^15.4.2",
    "react-bootstrap": "^0.30.8",
    "react-dom": "^15.4.2"
  },
  "devDependencies": {
    "bootstrap": "^3.3.7",
    "react-scripts": "0.9.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "deploy": "surge -p build -d resume.pnwnelson.com",
    "eject": "react-scripts eject"
  }
}
